# Example workflow recipe — RTK survey to CAD/GIS (roadmap Phase 5).
#
# A STARTER template: the command SEQUENCE is real and validated; fill each
# step's `values:` with that command's options and your paths (see
# `envmon <command> --help`). Validate with `envmon validate-recipe`, run with
# `envmon run-recipe`. `command: null` steps are review checkpoints.
version: 1
name: "RTK survey to CAD"
steps:
  - command: [envmon, validate-rtk-survey]
    values: {}            # _TODO --input / --input-format ...
    fail_on: error
    message: "Validate the RTK survey point file before import."

  - command: [envmon, import-rtk-survey]
    values: {}            # _TODO --gdb / survey inputs ...
    message: "Import RTK survey points into the geodatabase."

  - command: [envmon, survey-to-well-elevation]
    values: {}            # _TODO --gdb ...
    message: "Update well elevations from the survey (tool 8.5)."

  - command: null
    message: "Review the updated well elevations before export."

  - command: [envmon, export-survey-cad]
    values: {}            # _TODO --output / feature-code map ...
    message: "Export the survey features to CAD/GIS."
