# ============================================================================
# PARSER PROFILE — __SITE_NAME__ Data Tables        *** DRAFT — UNVERIFIED ***
# ============================================================================
# Generated by `envmon init-site`. The row/column anchors below are PLACEHOLDER
# guesses, not measured against a real workbook.
#
# REQUIRED BEFORE FIRST IMPORT:
#   1. Run Tool 1 (Inspect Environmental Workbook) on the real workbook.
#   2. Compare every placeholder row/column anchor below against the report.
#   3. Remove this banner and the DRAFT_ profile_id prefix once verified.
# Importing with an unverified profile risks misreading screening rows as data
# or shifting analyte columns — QA catches some, not all, of that.
# ============================================================================

profile_id: DRAFT___SITE_ID___DataTables
profile_description: >-
  DRAFT parser-profile skeleton for __SITE_NAME__. All anchors are placeholders
  pending Tool 1 inspection of the real workbook.
workbook_type: quarterly_gw_monitoring
date_system: "1900"
formula_handling: use_cached_then_qa

sheets:
  - sheet_name: "_TODO sheet name e.g. GW Table 2"
    data_type: GW_ANALYTICAL_AND_WATER_LEVEL
    title_rows: [1, 2, 3]
    group_header_row: 5          # _TODO verify against Tool 1 inspection
    analyte_header_row: 6        # _TODO verify
    units_row: 7                 # _TODO verify
    screening_level_row: 8       # _TODO verify
    data_start_row: 9            # _TODO verify
    data_end_detection_strategy: "consecutive_blank_ids:3"
    id_column: A                 # _TODO verify — location (well) ID column
    date_column: C               # _TODO verify
    analyte_columns: {from: G, to: V}   # _TODO verify first/last analyte column
    duplicate_markers: ["DUP", "-D", "FD"]
