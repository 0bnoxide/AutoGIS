# Event configuration ("schedule") for __SITE_NAME__.
# Generated by `envmon init-site`. Copy per monitoring event and fill values
# before generating a sampling-event plan (Tool 7.1a). Every analyte name must
# match the analyte dictionary — misspellings are caught before the field.
#   analyte_groups  — ordered map of group name -> list of canonical analyte names
#   crew_list       — names shown in the "Sampled By" dropdown
#   coc_prefix      — prefix for Chain of Custody numbers
#   matrices        — sample matrices collected this event
#   location_ids    — well/boring IDs that appear in the "Well ID" dropdown

event_name: "_TODO e.g. 2026 Q1 Groundwater Monitoring"
event_date: "_TODO YYYY-MM-DD"
coc_prefix: "__SITE_ID__-COC"
lab_name: "_TODO laboratory name"

matrices:
  - GW

location_ids:
  - "_TODO MW-1"

crew_list:
  - "_TODO Field Technician"

analyte_groups:
  "_TODO Analyte Group Name":
    - "_TODO Benzene"

group_sampling: {}
dup_frequency: 10          # duplicate every Nth location; 0 = none
